# Dependencias
node_modules/

# Variables de entorno (nunca subir claves)
.env
*.env

# Capacitor / Android (se generan al compilar en la nube, no se suben)
android/
.gradle/

# Sistema
.DS_Store

# Nota: la carpeta subscriptions/ SÍ se sube (con su .gitkeep).
# La app irá añadiendo ahí un .json por dispositivo automáticamente.
